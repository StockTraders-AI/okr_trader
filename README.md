# OKR Trader Web

Ứng dụng React/Vite tách component từ mẫu OKR Trader ban đầu.

## Chạy local

```bash
npm install
npm run dev
```

## Build và chạy bản build

```bash
npm run build
npm run start
```

Demo login:

- Admin: `admin` / `123`
- Trader mẫu: `minhanh` / `123`
